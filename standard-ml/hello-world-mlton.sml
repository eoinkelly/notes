(*
 * ############# MLton hello world #################
 *
 * * mlton will evaluate anything at the top level (it treats the sml like a "script"
 *   rather than expecting a main function.
 *
 * Build this with:
 *
 *     $ mlton -o hello-mlton ./hello-world-mlton.sml
 *
 *)
print "Hello, World!\n";

