```sh

ls **/*.rb # find all ruby files under current dir
ls [abc]* # find all files that begin with a, b, or c

ls *.{md,markdown}

# get all js and html files except those in the named dirs
ls */!(node_modules|bower_components)/*.{js,html}
```
