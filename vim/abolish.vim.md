# abolish.vim

```
crs = to snakecase
cr. = to dot case foo.bar.blah (one way because . separates words in vim)
cr<space> = to space case (one way because <space> separates words)
crm = mixed case ThisIsMixedCase
crc = to camelCase thisIsCamelCase
cr- = to dash case this-is-dash-case (one way conversion because - separates words in vim
cru = to UPPER_SNAKE_CASE this_is_upper_case
```
